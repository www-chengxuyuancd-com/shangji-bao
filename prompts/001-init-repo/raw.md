## 需求

帮我新建一个repo，采用python+scrapy+docker+postgresql+Flask+mongodb+prisma（如果可以，不行的话就用其他合适的ORM框架，我期望每次数据库结构的变更都可以反应出来）的构建方式

用来根据特定的关键词来检索结果，并将结果的原网页，以及hash值，存储进mongodb，解析的算法以及程序后面再想办法，先预留一个模块用来解析，

注意网页的地址需要存储下来避免二次访问和下载，

id都用bigint，不用uuid，

需要分为前端网页以及后台网页，前端网页用来让用户检索，后端网页用来让管理员设置检索的关键词，以及需要查询的地区，

前端网页的设计风格参考 ./images/1.png

最后部署需要用docker来部署，也就是我可以直接将docker image发过去或者在另一台电脑启动

python包管理使用 uv
