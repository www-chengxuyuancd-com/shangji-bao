## 需求

现在需要在后台有一个开始抓取的按钮，以及一个配置抓取任务频率的页面，可以配置每天抓取，还是每周一次，还是一周多次，以及星期几开始，或者每天几次，几点开始

还需要展示每次抓取任务的进度
抓取的时候，每个入口都需要去抓取一次：
对于搜索引擎：
选择使用 地区+关键词 匹配的方式，抓取前 10 页数据（可以配置）
存储搜索到的页面的url，url需要md5存储一个值，放在库里面避免重复抓取，
页面的原始信息，
url的网站域名地址，方便后期归类，
created_at,updated_at
这些新字段需要用prisma管理起来

对于网站类型，
需要通过网站首页，来才去广度搜索来遍历，最大深度为5（可以配置）
